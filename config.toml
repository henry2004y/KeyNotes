[projects]

  [projects.default]
  contents = [
    "about",
    "introduction",
    "single",
    "fluid",
    "wave",
    "kinetic",
    "nonlinear",
    "appendix",
    "references",
  ]

  # Full URL, required for the sitemap and robots.txt.
  online_url = "https://booktemplate.huijzer.xyz"

  # Extra directories to be copied.
  extra_directories = [
    "images",
  ]

  # Port used by serve()
  port = 8004

  output_filename = "keynotes"
